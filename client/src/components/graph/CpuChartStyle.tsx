import { ResponsiveContainer } from 'recharts';
import styled from 'styled-components';

export const GraphResponsiveContainer = styled(ResponsiveContainer)`
    margin: 1em;
    font-family: ForcedSquare;
    font-size: 1.2em;
`;
